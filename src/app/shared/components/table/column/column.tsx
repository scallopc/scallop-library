import React from "react";
import { ColumnProps } from "../table.model";

export function Column({ field, header, expander }: ColumnProps) {
     return null; // O componente Column não renderiza nada diretamente
}
